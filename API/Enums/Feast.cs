namespace Katameros.Enums
{
    // Each value correspond to the Id in the Feasts Table
    public enum Feast
    {
        Christmas = 1,
        Ascension = 2,
        LazarusSaturday = 3,
        PalmSunday = 4
    }
}
